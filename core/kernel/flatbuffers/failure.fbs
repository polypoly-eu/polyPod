namespace failure;

enum FailureCode: byte {
    FailedToBootstrapKernel = 1,
    KernelNotBootstraped,
    FailedToParseFeatureManifest,
}

table Failure {
    // Flatbuffers require to have a default value for enums
    code:FailureCode = FailedToBootstrapKernel;
    message:string;
}