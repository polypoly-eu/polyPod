// construction site

/*import React, { useEffect, useRef } from "react";

//Component takes profit values from 1 or 2 years
const RevenueBarChart = ({ width, height, data1, data2 }) => {
    let profitRef = useRef(null);

    const months = [
        "Jan",
        "Feb",
        "Mär",
        "Apr",
        "Mai",
        "Jun",
        "Jul",
        "Aug",
        "Sep",
        "Okt",
        "Nov",
        "Dez",
    ];

    useEffect(() => {});

    return <div width={width} height={height} ref={profitRef} />;
};

export default RevenueBarChart;
*/
