{
  "name": "demo-feature",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "build": "cp -r src dist && cp node_modules/@polypoly-eu/podjs/dist/pod.js dist"
  },
  "devDependencies": {
    "@polypoly-eu/podjs": "file:../../podjs"
  },
  "private": true
}
